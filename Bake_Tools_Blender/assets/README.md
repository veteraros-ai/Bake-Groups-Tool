# UI assets

The original PNG assets have been copied here without resizing or recompressing them. They are loaded as Blender preview icons by `addon/bake_tools_blender/icons.py`.

Keep original filenames because `bg_ui_widgets.get_icon()` and the source UI reference them by name.
